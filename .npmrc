# Enforce lockfile to prevent dependency confusion attacks
package-lock=true

# Fail on audit issues
audit=true

# Don't run scripts from dependencies on install
ignore-scripts=false

# Use highest security settings for npm
fund=false